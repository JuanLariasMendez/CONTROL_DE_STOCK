package com.alura.jdbc.modelo;import java.util.ArrayList;import java.util.List;public class Categoria {    //Campos de la tabla en la BDD    private Integer id;    private String nombre;    //Variable para poder utilizar la relacion muchos a 1    //Una categoria puede tener muchos productos, pero un producto puede tener 1 categoria    private List<Producto> productos;    //Costructor, Indica la primera categoria dentro del form    public Categoria(int id, String nombre) {        this.setId(id);        this.nombre=nombre;    }    public Integer getId() {        return this.id;    }    public void setId(Integer id) {        this.id = id;    }    //Sobreescritura del método toString para que nos transforme el valor guardado en memotia de las categorias a texto legible    @Override    public String toString() {        return this.nombre;    }    public void agregar(Producto producto) {        if(this.productos == null){            this.productos = new ArrayList<>();        }        this.productos.add(producto);    }    public List<Producto> getProductos() {        return this.productos;    }}